# Backend / container
PORT=4000
HOST=0.0.0.0
FRONTEND_ORIGIN=*
DATA_FILE=./data/store.json
# PERSIST=0
SERVE_FRONTEND=1
# FRONTEND_DIST=../frontend/dist
# Canonical / Open Graph origin (boş = Host + X-Forwarded-* )
# PUBLIC_ORIGIN=https://rps.example.com

# Frontend (build-time). Leave empty for same-origin / Docker.
VITE_SOCKET_URL=
